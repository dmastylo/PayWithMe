<% provide(:title, 'Request Payment') %>
<div class="page-header">
  <h2>Create Payment Request</h2>
</div>

<%= form_for @payment do |f| %>
  <%= render 'fields', object: f %>

  <div class="form-actions">
    <%= f.submit "Request Payment", class: 'btn btn-primary' %>
  </div>
<% end %>