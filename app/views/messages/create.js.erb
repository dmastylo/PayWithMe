$('.no-messages').remove();
$('.message-list').prepend('<%= j render(@message) %>');
$('.message-text-field').val('');
temporarySpamBlock();
$('.message#message-<%= @message.id %>').autoSize({
	fixed: '.profile-image',
	fluid: '.body'
});