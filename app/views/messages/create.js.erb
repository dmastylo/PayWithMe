$('.no-messages').remove();
$('.message-list').prepend('<%= j render(@message) %>');
$('.message-text-field').val('');
// Keep a running count of messages
<% @messages_count = @messages_count.to_i + 1 %>
temporarySpamBlock();