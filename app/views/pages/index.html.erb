<% if user_signed_in? %>
  <div class="row">
    <div class="span8">
      <h1>News Feed</h1>
      Content to be decided
    </div>
    <div class="span4">
      <%= link_to "Create New Event", new_event_path, class: "btn btn-block btn-primary" %>
      <h3 class="push-top">Upcoming Events</h3>
      <%= render @upcoming_events %>
    </div>
  </div>
<% else %>
  nothing
<% end %>