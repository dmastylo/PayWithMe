<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "PayWithMe" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "PayWithMe", root_path, class: "brand" %>
          <% if current_user %>
            <%= form_tag(search_users_path, { method: :post, class: "navbar-search pull-left", id: "search-form" }) do %>
              <div class="pull-left">
                <input type="text" class="search-query span4" id="search-friends" placeholder="Search for friends..." />
              </div>
              <div class="dropdown pull-left navbar-notifications">
                <a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#">
                  <i class="icon-globe"></i>
                  <% if current_user.has_unread_notifications? %>
                    <div class="unread-notifications-count">
                      <%= current_user.unread_notifications.count %>
                    </div>
                  <% end %>
                </a>
                <ul class="dropdown-menu" role="menu">
                  <% current_user.current_notifications.each do |notification| %>
                    <li <%= 'class="unread"'.html_safe unless notification.read? %>><a href="<%= notification.url %>"><%= notification.body %></a></li>
                  <% end %>

                  <% if current_user.current_notifications.empty? %>
                    <li><a href="#">You have no unread notifications.</a></li>
                  <% end %>
                </ul>

              </div>
            <% end %>
          <% end %>
          <div class="container nav-collapse">
            <ul class="nav pull-right">
              <% if current_user %>
                <li>
                  <% image = image_tag avatar_url(current_user, size: 24), class: 'navbar-profile-image' %>
                  <%= link_to image + current_user.name, user_path(current_user), class: 'pull-right'  %>
                </li>
                <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span12">
              <%= content_tag(:div, flash[:error], :class => 'alert alert-error', :id => "flash_error") if flash[:error] %>
              <%= content_tag(:div, flash[:notice], :class => 'alert alert-warning', :id => "flash_notice") if flash[:notice] %>
              <%= content_tag(:div, flash[:alert], :class => 'alert alert-info', :id => "flash_alert") if flash[:alert] %>
            </div>
        </div><!--/row-->
        <%= yield %>
      </div><!--/content-->

      <footer>
        <p>&copy; PayWithMe 2012</p>
      </footer>

      <%= debug(params) if Rails.env.development? %>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>


  </body>
</html>
