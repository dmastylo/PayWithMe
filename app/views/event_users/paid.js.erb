$('.unpaid').remove();
$('#user-status-<%= @event_user.member.id %>').html('<%= j distance_of_time_in_words_to_now @event.paid_at(@event_user.member) %> ago');
$('#payment-links').html('<%= link_to "Mark Unpaid", unpaid_event_event_user_path(@event, @event.event_user(@event_user.member)), class: "btn btn-success btn-block", method: :put, remote: true %>');
